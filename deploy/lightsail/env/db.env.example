# Copy to /etc/zurik/db.env
# POSTGRES_PASSWORD is the raw password. URL-encode it only in DATABASE_URL.
# Changing these after the first start does not alter an existing data volume.

POSTGRES_DB=african_fashion_db
POSTGRES_USER=zurik
POSTGRES_PASSWORD=replace-with-openssl-rand-base64-32
